package at.htl.karate.boundary;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRestaurantResourceIT extends RestaurantResourceTest {

    // Execute the same tests but in native mode.
}